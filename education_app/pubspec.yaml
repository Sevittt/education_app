name: education_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0 # Ensure this is compatible with your Flutter version

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: # Add this for localization
    sdk: flutter         # Add this for localization

  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.1 # Or latest
  provider: ^6.1.5    # Or latest
  uuid: ^4.5.1        # Or latest
  shared_preferences: ^2.5.3 # Or latest
  # Firebase Core for initializing Firebase
  firebase_core: ^3.13.0   # Or latest
  # Firebase Authentication
  firebase_auth: ^5.5.3   # Or latest
  flutter_bloc: ^8.1.1
  hydrated_bloc: ^9.0.0
  intl: 0.19.0
  path_provider: ^2.0.9
  equatable: ^2.0.3
  # Cloud Firestore (we'll use this later for user data)
  cloud_firestore: ^5.0.2 # Or latest
  flutter_speed_dial: ^7.0.0 # Add this line

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0 # Or latest
  
flutter:
  uses-material-design: true
  generate: true # Add this line to enable code generation for localization

  assets:
    - assets/images/Shared_Knowledge.png
    - assets/images/Teach&Learn Logo.png
    - assets/images/solijonov_madamin.jpg
    - assets/images/my_pic.jpg